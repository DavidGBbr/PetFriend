export interface PetType {
  id: string;
  ownerId: string;
  name: string;
  picture: string;
  specie: string;
  weight: number;
  age: number;
  whatsapp: string;
  city: string;
  description?: string;
}
